/// Facts about the build shown in the About section.
abstract final class AppInfo {
  static const String name = 'Headline News';

  /// Keep in step with `version` in pubspec.yaml.
  static const String version = '1.0.0';
}
